# All dependencies (pinned to verified-working versions) are declared in
# pyproject.toml. This file exists only for tools/workflows that expect a
# requirements.txt to be present -- `pip install -r requirements.txt` and
# `pip install -e .` do exactly the same thing.
-e .
